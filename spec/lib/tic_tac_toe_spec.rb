## An example test to demonstrate that tests were setup properly
## To be removed when first tests are written

RSpec.describe "tic-tac-toe" do
  context "make sure tests are working" do
    it "should pass" do
      expect(1).to eql(1)
    end
  end
end